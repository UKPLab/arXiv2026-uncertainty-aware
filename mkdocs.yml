site_name: arxiv2026_uncertainty_aware
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2026_uncertainty_aware
      handlers:
        python:
          options:
            docstring_style: numpy